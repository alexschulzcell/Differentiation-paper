# 01_expression_landscape — The internal RNA layer as a continuous per-gene quantity

Turns the yes/no convergence list into a graded per-gene quantity across the 18 perturbation data sets — the substrate the programme is later derived from.

## Scripts

- `11_internal_gene_map.py` — _needs raw data_

_“repo-runnable” = reproduces from files in this repository alone. “needs raw data” = archival; requires the ~98 GB public raw data under `data_raw/` (see `00_setup.md`)._
